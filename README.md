node-chat-demo (socket.io)
===============

A very simple chat demo using websocket. Using socket.io instead of ws modules.

Install
==================

```{bash}
cd node-chart-demo
npm install
node app.js
```

